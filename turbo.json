{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["**/.env.*local"],
	"ui": "tui",
	"tasks": {
		"start": {},
		"build": {
			"dependsOn": ["^build"],
			"outputs": []
		},
		"@lucidcms/admin#build": {
			"dependsOn": ["^build"],
			"outputs": ["../core/spa/**"]
		},
		"@lucidcms/core#build": {
			"dependsOn": ["^build", "@lucidcms/admin#build"],
			"outputs": []
		},
		"lint": {},
		"lint:fix": {},
		"format": {},
		"format:fix": {},
		"check": {},
		"check:fix": {},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
