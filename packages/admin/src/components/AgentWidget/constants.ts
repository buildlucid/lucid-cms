/** A single renderer handles each widget key and version. */
export const agentWidgetSlotPolicies = {
	"agent.widget": {
		surface: "agentWidget",
		multiple: false,
		group: "agent.widget",
	},
} as const;
