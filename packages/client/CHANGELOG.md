# @lucidcms/client

## 0.2.0

- Structured sort descriptors now use `direction` instead of `value`, for example `{ key: "updatedAt", direction: "desc" }`.

## 0.1.1

- Bumped `@lucidcms/types` to ^0.2.0.

## 0.1.0

- The initial release of the client.
